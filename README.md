# STM32F446RE-GPIO-Drivers
Basic GPIO drivers for the STM32F446RE using only C and CMSIS libraries (no HAL). Capable of basic configuration, read/write operations, and external interrupts.
My work so far is found in ~/Core/Inc and ~/Core/Src in gpio.c/.h and interrupt.c/.h
